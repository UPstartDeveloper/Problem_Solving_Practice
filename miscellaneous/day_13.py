"""Simple Question"""


def compute_new_array(a, k, f):
    pass


if __name__ == "__main__":
    a = [1, 2, 3, 4, 5]
    k = 2

    def increment(element, index):
        return element + 2

    print(compute_new_array(a, k, increment))

"""
use the range function to compute all possible x and y values for intersection
use indicies in the range functions to compute the distances
rect1_y_range = range(1, 5)  = i
rect2_y_range = range(2, 7)  = j
"""
